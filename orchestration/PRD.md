# PRD — Source of Truth

The canonical PRD is **[`../GOLM-LeadGen-Funnel-PRD.md`](../GOLM-LeadGen-Funnel-PRD.md)**.

It is kept as a single file to avoid drift. Do not duplicate its contents here.
Agents read the canonical file directly. This pointer exists so the document
hierarchy in `PROTOCOL.md` has a stable `PRD.md` entry point.

If the PRD is ever revised, the canonical file is the only place it changes.
